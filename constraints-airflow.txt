# Constraints file for the Airflow Docker image.
# Prevents pip from upgrading apache-airflow beyond the base image version.
# Passed via: pip install --constraint constraints-airflow.txt -r requirements-airflow.txt
apache-airflow==2.10.3
