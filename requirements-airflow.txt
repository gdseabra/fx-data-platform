# Minimal dependencies for the Airflow container (compatible with apache/airflow:2.10.3).
# apache-airflow is NOT listed here — it is already in the base image.
# It IS listed in constraints-airflow.txt (passed via --constraint) so pip
# cannot upgrade it to 3.x when resolving provider transitive dependencies.
# Providers are pinned to <6.0.0 because 6.x requires Airflow 3.x.

# Spark
pyspark==3.5.0

# AWS / Storage
boto3>=1.34.0,<2.0.0

# Database driver (psycopg2 is already in the base image, but pin for clarity)
psycopg2-binary>=2.9.0,<3.0.0

# Caching
redis>=5.0.0,<6.0.0

# Airflow providers — must be <6.0.0 to stay compatible with Airflow 2.10.x
apache-airflow-providers-apache-spark>=4.5.0,<5.0.0
apache-airflow-providers-postgres>=5.10.0,<6.0.0
apache-airflow-providers-http>=4.9.0,<5.0.0
apache-airflow-providers-amazon>=8.0.0,<9.0.0

# Shared project utilities
confluent-kafka>=2.3.0,<3.0.0
jsonschema>=4.20.0,<5.0.0
pydantic>=2.0.0,<3.0.0
structlog>=23.3.0,<26.0.0
python-dotenv>=1.0.0,<2.0.0
tenacity>=8.2.0,<10.0.0
